services:
  amo_server:
    build: ./server/
    container_name: amo_server
    ports:
      - "80:80"
    env_file:
      - .env  

  telegram_bot:
    build: ./TG_bot/
    container_name: telegram_bot
    depends_on: 
      - amo_server
    env_file:
      - .env 