services:
  amo_server:
    build: ./server/
    container_name: amo_server
    volumes:
      - ./Data:/Data
    ports:
      - "80:80"
    env_file:
      - .env  

  telegram_bot:
    build: ./TG_bot/
    container_name: telegram_bot
    volumes:
      - ./Data:/Data
    depends_on: 
      - amo_server
    env_file:
      - .env 